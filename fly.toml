app = "aegis-backend"
primary_region = "sjc"  # San Jose — closest to you

[build]

[env]
  AEGIS_DB_PATH = "/data/aegis.duckdb"
  AEGIS_DATA_DIR = "/data/aegis"
  AEGIS_REFRESH_INTERVAL_HOURS = "6"

[mounts]
  source = "aegis_data"
  destination = "/data"

[[services]]
  internal_port = 8000
  protocol = "tcp"

  [[services.ports]]
    handlers = ["http"]
    port = 80

  [[services.ports]]
    handlers = ["tls", "http"]
    port = 443

  [services.concurrency]
    hard_limit = 25
    soft_limit = 20
    type = "connections"

  [[services.http_checks]]
    interval = "60s"
    timeout = "30s"
    grace_period = "30s"
    method = "GET"
    path = "/v1/health"

[vm]
  memory = "2048mb"
  cpu_kind = "shared"
  cpus = 1
